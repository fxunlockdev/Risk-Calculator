# Supabase (anon key is safe to expose — access is enforced by RLS)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set to "/Risk-Calculator" only when building for GitHub Pages
NEXT_PUBLIC_BASE_PATH=
